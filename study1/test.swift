import SwiftUI

struct IntroduceView: View {    var body: some View {
        VStack(spacing: 20) {
            Text("Hello World")
        }
        .padding()
    }
}

#Preview {
    IntroduceView()
}
